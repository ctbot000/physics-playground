{
  "name": "physics-playground",
  "version": "1.0.0",
  "private": true,
  "description": "A 2D rigid-body physics sandbox that runs in the browser with no dependencies and no build step.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "python3 scripts/dev-server.py 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
